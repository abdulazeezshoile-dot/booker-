Booker UI - React Native (Expo) UI-only scaffold

Getting started

Install dependencies:

```bash
cd /workspaces/booker-
npm install
npm start
```

This repo contains UI screens and components for a bookkeeping & inventory mobile app.
# booker-
bookimg management
